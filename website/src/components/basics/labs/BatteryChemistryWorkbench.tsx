export default function BatteryChemistryWorkbench() {
  return (
    <div
      className="w-full rounded-lg p-8 flex items-center justify-center font-mono text-sm"
      style={{
        background: 'var(--color-bg-alt)',
        border: '1px solid var(--color-surface-light)',
        color: 'var(--color-text-dim)',
        minHeight: 200,
      }}
    >
      Battery Chemistry Workbench -- coming soon
    </div>
  );
}
