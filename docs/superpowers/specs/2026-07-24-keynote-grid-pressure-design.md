# Keynote grid-pressure and closing assets

## Change

Replace slide 5 title `This Is Not the First Warning` with `Grid pressure`.

On final slide, replace `100K HOMES` with `100K DEVICES` and add a small animated SVG row for a home, solar panel, EV, and battery. Icons use existing wash-paper color tokens and a gentle staggered float/pulse; the existing supporting copy and contact line remain.

## Scope

Keep slide 5 subtitle unchanged. Keep final-slide supporting copy, contact line, slide order, and all other keynote behavior unchanged.

## Verification

Run focused keynote rendering test; assert final heading and all four asset icons exist.
